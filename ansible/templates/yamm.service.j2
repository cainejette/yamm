[Unit]
Description=YAMM
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/node /app/{{ build_number }}/src/server.js && DISPLAY=:0 /usr/bin/midori -e Fullscreen -a http://localhost
StandardOutput=journal
Environment=YAMM_PORT={{ port }}

[Install]
WantedBy=multi-user.target
