{
  "name": "yamm",
  "scripts": {
    "clean": "rm -rf out/",
    "build": "mkdir -p out && rsync -av --progress src/* out && cp package*.json out",
    "start": "node out/server.js"
  },
  "dependencies": {
    "angular": "^1.7.9",
    "angular-animate": "^1.5.5",
    "cheerio": "^0.20.0",
    "curlrequest": "^0.5.7",
    "express": "^4.13.4",
    "http-proxy": "^1.13.2",
    "moment": "^2.12.0",
    "weather-icons": "^1.3.2"
  }
}